const fs = require('fs'); let css = fs.readFileSync('style.css', 'utf8'); css = css.replace(/.skill-item input\[type=\"number\"\].*/, '.skill-item input[type=\"number\"] { width: 60px; text-align: center; padding-right: 15px !important; flex-shrink: 0; }'); fs.writeFileSync('style.css', css, 'utf8');
