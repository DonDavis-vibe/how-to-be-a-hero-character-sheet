const html=require('fs').readFileSync('index.html','utf8').split('\n'); let d=0; for(let i=0;i<html.length;i++) { const line=html[i]; const opens=(line.match(/<div\b/g)||[]).length; const closes=(line.match(/<\/div>/g)||[]).length; d += opens; d -= closes; if(line.includes('class="app-container"')||line.includes('class="character-sheet')||line.includes('class="tools-panel"')) console.log(i+1, line.trim(), 'depth=', d); if(d===0 && closes>0) console.log('REACHED 0 AT LINE', i+1); }
